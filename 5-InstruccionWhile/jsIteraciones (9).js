function mostrar()
{

	var contador=0;
	var numeros;
	var maximo;
	var minimo;
	var respuesta='s';	// declarar variables
	
	do
	{
		numero = parseInt(prompt("Ingrese un numero: "));
		while(isNaN(numero))
		{
			alert("Error, eso no es un numero, ingrese nuevamente un numero");
			numero = parseInt(prompt("Ingrese un numero: "));
		}

		if(contador                                                                                                                                                                                         )


	}while(respuesta!='n');




}//FIN DE LA FUNCIÓN